[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "loop-doctor"
version = "0.0.1"
description = "Pre-flight readiness auditor for the four pipeline."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "belarusian" }]
keywords = ["pre-flight", "readiness", "auditor", "ci", "pipeline"]
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: OS Independent",
    "Environment :: Console",
    "License :: OSI Approved :: MIT License",
]
dependencies = []

[project.optional-dependencies]
full = [
    "spoke-lint @ git+https://github.com/belarusian/spoke-lint.git@ac8196327a7cf4987f8bf76d1b13da61b4f7fe5f",
    "fourseer @ git+https://github.com/belarusian/fourseer.git@6537691469b567d0c5e5858f32ad1152b2596e1d",
]

[project.scripts]
loop-doctor = "loop_doctor.cli:main"

[project.urls]
Homepage = "https://github.com/belarusian/loop-doctor"
Repository = "https://github.com/belarusian/loop-doctor"
Documentation = "https://github.com/belarusian/loop-doctor/blob/main/docs/architecture.md"
Issues = "https://github.com/belarusian/loop-doctor/issues"

[tool.setuptools.packages.find]
include = ["loop_doctor*"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]

[tool.mypy]
ignore_missing_imports = true
python_version = "3.10"
